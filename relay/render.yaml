services:
  - type: web
    name: board-relay
    runtime: node
    plan: free
    rootDir: relay
    buildCommand: ""
    startCommand: node relay.js
    envVars:
      - key: RELAY_TOKEN
        generateValue: true    # a random token; send the same one from the portal
